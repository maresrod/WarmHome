<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--Diseño Firebase Login-->
    <style name="FirebaseUITema" parent="FirebaseUI">
    <item name="windowNoTitle">false</item>
    <item name="windowActionBar">true</item>
    <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="colorPrimary">#FF8800</item>
        <item name="colorPrimaryDark">#FF8800</item>
        <item name="colorAccent">#FF8800</item>
        <item name="colorControlNormal">#5E4F47</item>
        <item name="colorControlActivated">#FF8800</item>
        <item name="android:windowBackground">@drawable/backgrounprueba</item> <!-- o reemplaza por drawable-->
    </style>

    <style name="etiqueta">
        <item name="android:background">@drawable/fondo_eti_fab</item>
        <item name="android:textColor">#fff</item>
    </style>

    <style name="AppTheme.CustomTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
    </style>
</resources>
